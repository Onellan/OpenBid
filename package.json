{
  "name": "openbid-e2e",
  "private": true,
  "scripts": {
    "test:e2e": "docker compose -f ProductionDeployment/docker-compose.yml --profile e2e run --rm --no-deps e2e-seed && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "otplib": "13.4.0"
  }
}
