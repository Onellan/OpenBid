# Raspberry Pi Docker Setup

The Raspberry Pi Docker setup guide has been consolidated into the root [README.md](../README.md).

Use the README as the single source of truth for:

- Raspberry Pi prerequisites and OS recommendation
- Docker and Docker Compose installation
- environment variables and secret files
- build, start, access, update, backup, restore, and troubleshooting

This file is intentionally short so setup steps are not duplicated or allowed to drift.
